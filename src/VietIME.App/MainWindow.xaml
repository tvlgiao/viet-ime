<Window x:Class="VietIME.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="VietIME"
        Height="640" Width="420"
        MinHeight="400" MinWidth="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow"
        Background="{StaticResource BackgroundBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <StackPanel Margin="16">

        <!-- Status Card -->
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="18" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Logo + Name -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
                    <Border Width="42" Height="42" CornerRadius="10" Background="#14141C">
                        <TextBlock Text="V" FontSize="22" FontWeight="Bold" Foreground="#D72828"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="12,2,0,0" VerticalAlignment="Center">
                        <TextBlock Text="VietIME" FontSize="20" FontWeight="Bold"/>
                        <TextBlock Text="Bộ gõ Tiếng Việt cho Claude Code và Warp IDE" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,1,0,0"/>
                        <TextBlock Text="Tác giả: Đỗ Nam — Teacher THCS Duyên Thái" FontSize="10" Foreground="{StaticResource TextDimBrush}" Margin="0,2,0,0" FontStyle="Italic"/>
                    </StackPanel>
                </StackPanel>

                <!-- Toggle + Status -->
                <Grid Grid.Row="1" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ToggleButton x:Name="toggleEnabled" Grid.Column="0"
                                  Style="{StaticResource ToggleSwitch}"
                                  IsChecked="True"
                                  Checked="ToggleEnabled_Changed" Unchecked="ToggleEnabled_Changed"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                        <Ellipse x:Name="statusDot" Width="8" Height="8" Fill="{StaticResource SuccessBrush}" Margin="0,0,6,0"/>
                        <TextBlock x:Name="txtStatus" Text="Đang bật" FontSize="13"/>
                        <TextBlock Text=" — " Foreground="{StaticResource TextDimBrush}" FontSize="13"/>
                        <TextBlock x:Name="txtEngine" Text="Telex" FontSize="13" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold"/>
                    </StackPanel>
                </Grid>

                <!-- Notifications toggle -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ToggleButton x:Name="toggleNotifications" Grid.Column="0"
                                  Style="{StaticResource ToggleSwitch}"
                                  IsChecked="False"
                                  Checked="ToggleNotifications_Changed" Unchecked="ToggleNotifications_Changed"/>
                    <TextBlock Grid.Column="1" Text="Thông báo" FontSize="13" VerticalAlignment="Center" Margin="12,0,0,0"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Input Method Card -->
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="18" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="KIỂU GÕ" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,10"/>

                <RadioButton x:Name="rbTelex" IsChecked="True" Checked="InputMethod_Changed" GroupName="InputMethod">
                    <RadioButton.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Telex" FontWeight="SemiBold"/>
                            <TextBlock Text="  aa=â  dd=đ  s=sắc" Foreground="{StaticResource TextDimBrush}" FontSize="11"/>
                        </StackPanel>
                    </RadioButton.Content>
                </RadioButton>

                <RadioButton x:Name="rbVNI" Checked="InputMethod_Changed" GroupName="InputMethod">
                    <RadioButton.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="VNI" FontWeight="SemiBold"/>
                            <TextBlock Text="    6=â  9=đ  1=sắc" Foreground="{StaticResource TextDimBrush}" FontSize="11"/>
                        </StackPanel>
                    </RadioButton.Content>
                </RadioButton>
            </StackPanel>
        </Border>

        <!-- Shortcut Card -->
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="18" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="PHÍM TẮT" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,10"/>

                <!-- Shortcut Ctrl+Alt+Z -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Bật/tắt VietIME" FontSize="13" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="6" Padding="8,3">
                            <TextBlock Text="Ctrl" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                        </Border>
                        <TextBlock Text=" + " Foreground="{StaticResource TextDimBrush}" VerticalAlignment="Center" FontSize="11"/>
                        <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="6" Padding="8,3">
                            <TextBlock Text="Alt" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                        </Border>
                        <TextBlock Text=" + " Foreground="{StaticResource TextDimBrush}" VerticalAlignment="Center" FontSize="11"/>
                        <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="6" Padding="8,3">
                            <TextBlock Text="Z" FontSize="11" Foreground="{StaticResource TextMutedBrush}" FontFamily="Consolas"/>
                        </Border>
                    </StackPanel>
                </Grid>

                <Border Height="1" Background="{StaticResource SurfaceLightBrush}" Margin="0,4,0,8"/>

                <!-- Shortcut Ctrl+Alt+Shift+Z -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Tắt gõ tiếng Việt" FontSize="13" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="6" Padding="8,3">
                            <TextBlock Text="Ctrl" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                        </Border>
                        <TextBlock Text=" + " Foreground="{StaticResource TextDimBrush}" VerticalAlignment="Center" FontSize="11"/>
                        <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="6" Padding="8,3">
                            <TextBlock Text="Alt" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                        </Border>
                        <TextBlock Text=" + " Foreground="{StaticResource TextDimBrush}" VerticalAlignment="Center" FontSize="11"/>
                        <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="6" Padding="8,3">
                            <TextBlock Text="Shift" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                        </Border>
                        <TextBlock Text=" + " Foreground="{StaticResource TextDimBrush}" VerticalAlignment="Center" FontSize="11"/>
                        <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="6" Padding="8,3">
                            <TextBlock Text="Z" FontSize="11" Foreground="{StaticResource TextMutedBrush}" FontFamily="Consolas"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Warp-only Mode Card -->
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="18" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ToggleButton x:Name="toggleWarpOnly" Grid.Column="0"
                              Style="{StaticResource ToggleSwitch}"
                              IsChecked="False"
                              Checked="ToggleWarpOnly_Changed" Unchecked="ToggleWarpOnly_Changed"/>
                <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Chỉ bật trong Warp" FontSize="13"/>
                    <TextBlock Text="Khi active Warp: tự bật. App khác: tự tắt." FontSize="10" Foreground="{StaticResource TextDimBrush}" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Footer -->
        <Grid Margin="0,2,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock FontSize="10" Foreground="{StaticResource TextDimBrush}">
                    <Run Text="v1.0.0"/> <Run Text=" — "/> <Run Text="Đỗ Nam" FontWeight="SemiBold"/>
                </TextBlock>
            </StackPanel>

            <Button Grid.Column="1" Content="Ẩn xuống khay" Click="BtnMinimize_Click" Margin="0,0,6,0" Padding="14,8"/>
            <Button Grid.Column="2" Content="Thoát" Click="BtnExit_Click" Style="{StaticResource ButtonDanger}" Padding="14,8"/>
        </Grid>

    </StackPanel>
    </ScrollViewer>
</Window>
